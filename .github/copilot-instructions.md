# PhotoMap Processor - Инструкции для AI-агентов

## 🎯 Назначение проекта
Утилита для параллельной обработки фотографий: извлечение GPS-координат из EXIF-данных, создание миниатюр и генерация JSON-файла для интерактивной карты в браузере.

## 🏗️ Архитектура

### Основные компоненты
- **Основная библиотека**: `image` crate (поддержка JPEG, PNG, WebP, TIFF, BMP, GIF)
- **Параллелизм**: `rayon::par_iter()` для обработки файлов
- **EXIF парсинг**: `kamadak-exif` (импортируется как `exif` - см. Cargo.toml)
- **Обход файлов**: `ignore` crate для уважения `.gitignore`

### Поток данных
```
Файлы на диске 
  → ignore::Walk (параллельный сбор)
  → rayon::par_iter (параллельная обработка)
  → EXIF извлечение (GPSLatitude/Longitude)
  → create_thumbnail() (генерация миниатюр в `.thumbnails/`)
  → JSON сериализация → geodata.js
  → map.html отображает результат
```

## 📋 Критические детали

### Поддерживаемые форматы
**Текущая версия (без HEIC поддержки):** `jpg`, `jpeg`, `png`, `tiff`, `tif`, `webp`, `bmp`, `gif`
- Проверка в `process_file()` (src/main.rs)
- Любой другой формат → ошибка (не обрабатывается)

**Примечание про HEIC**: Добавление поддержки HEIC требует установки на Windows:
- Visual Studio Build Tools (для компиляции C-кода libheif)
- ИЛИ система Linux/Mac с установленным GCC/Clang
- Без этого используйте текущую версию без HEIC поддержки

### GPS-координаты из EXIF
- Теги: `Tag::GPSLatitude/GPSLongitude` + референсы `GPSLatitudeRef/GPSLongitudeRef`
- Формат хранения: `[degrees, minutes, seconds]` (рациональные числа)
- Конвертация: `decimal = deg + (min/60.0) + (sec/3600.0)`
- Применение знака: S/W → отрицательные значения

### Архитектура миниатюр
- Каталог: `.thumbnails/` (константа `THUMBNAIL_DIR`)
- Размер: `400x400` px максимум (константа `THUMBNAIL_SIZE`)
- Метод: `img.thumbnail()` сохраняет пропорции (не crop)
- Имена файлов: заменяются `/` и `\` на `_` для безопасности

### Вывод результата
- Файл: `geodata.js` (переменная `OUTPUT_FILE`)
- Формат: JavaScript-переменная `var photoData = [...]`
- Метод записи: `serde_json::to_writer_pretty()` потоком (не буферизирует всё в памяти)
- **HTML карта**: `map.html` генерируется автоматически если отсутствует

## 🔧 Рабочий процесс
```bash
cargo build --release    # Сборка с оптимизацией (3.44 МБ, 56% сжатия)
cargo run --release      # Запуск с результатами в geodata.js
```

**Оптимизация размера:**
- Использованы флаги: `opt-level = "z"`, `lto = "fat"`, `strip = true`
- Результат: 7.8 МБ → 3.44 МБ (**56% сжатия**)
- Скорость обработки практически не изменилась

### Распространение программы

Для раздачи конечным пользователям нужен **только 1 файл**:
1. `photomap_processor.exe` (из `target/release/`)

**Структура папки для конечного пользователя:**
```
PhotoMap/
└── photomap_processor.exe
```

Пользователь:
1. Кладёт фотографии в эту папку
2. Запускает `photomap_processor.exe` - **программа сама создаст map.html**
3. Открывает `map.html` в браузере

**Так просто!** HTML генерируется встроенным кодом при первом запуске.

**Примечание на Windows**: Если у вас установлены Visual Studio Build Tools и нужна поддержка HEIC, добавьте в Cargo.toml:
```toml
libheif-rs = { version = "2.1", default-features = false, features = ["image", "embedded-libheif"] }
```

### Тестирование локально
1. Положить JPEG/PNG/HEIC с GPS-данными в текущий каталог
2. Запустить `cargo run --release`
3. Проверить: `.thumbnails/` и `geodata.js` созданы
4. Открыть `map.html` в браузере

## 💡 Паттерны и соглашения

### Обработка ошибок
- Используется `anyhow::Result` и `with_context()` для контекста
- Параллельная обработка: `filter_map(|path| process_file(path).ok())` игнорирует ошибки
- Не падает при неподдерживаемых форматах или отсутствии GPS

### Логирование
- Простые `println!` с эмодзи для ориентира: `🗺️`, `📦`, `✅`, `🔍`, `🎉`, `✋`
- Нет dependency на специализированный логер
- **Программа ждет нажатия клавиши перед закрытием** (для видимости результатов при запуске .exe)

### Параллелизм
- Rayon заботится о thread pool, явно никуда не пуш/джойним
- Файлы обрабатываются независимо (нет race conditions)
- Результаты собираются в `Vec` после завершения всех тредов

## 🔗 Ключевые файлы
- `src/main.rs`: вся логика в одном файле (~180 строк)
- `Cargo.toml`: `kamadak-exif` импортируется как `exif` через `package` field
- `map.html`: фронтенд (не трогать из Rust-кода)
- `.thumbnails/`: генерируется автоматически, безопасно удалить
