# PhotoMap Quick Reference

## 🚀 Запуск проекта
```bash
cargo run
# Сервер запустится на http://127.0.0.1:3001 или 3002
```

## 🗂️ Выбор папки (Обзор кнопка)
- **Работает**: ✅ Браузерный нативный визард открывается
- **Решение**: HTML5 File API с webkitdirectory
- **Преимущества**: Нет внешних зависимостей, кроссплатформенный

## 📱 HEIC Конвертация
- **Маркеры**: `40x40>` - сохраняет пропорции
- **Миниатюры**: `60x60>` - сохраняет пропорции
- **ЗАПРЕЩЕНО**: `40x40^` или `60x60^` (искажает пропорции)

## 📁 Структура проекта
```
photomap/
├── src/
│   ├── main.rs                 # Основная логика
│   ├── server.rs               # HTTP API
│   ├── folder_picker.rs        # Устаревший модуль выбора папок
│   ├── image_processing.rs     # HEIC обработка (правильные параметры)
│   └── html_template.rs        # Web интерфейс с webkitdirectory
├── SOLUTIONS.md                # Подробная документация
└── photos/                     # Папка с фотографиями
```

## 🔧 Критически важные проверки

### 1. Проверить выбор папок в браузере
```bash
cargo run
# Открыть http://127.0.0.1:3001
# Нажать кнопку "Обзор"
# Должен открыться системный диалог выбора папки
```

### 2. Проверить HEIC параметры
В `src/image_processing.rs` убедиться:
```rust
"marker" => vec![..., "-resize", "40x40>", ...]
"thumbnail" => vec![..., "-resize", "60x60>", ...]
```

### 3. Проверить API
```bash
curl http://127.0.0.1:3001/api/photos
# Должен вернуть список фотографий с GPS
```

## 🚨 Запрещенные подходы
1. ❌ ImageMagick параметры с `^`
2. ❌ Изменение размеров без синхронизации

## 📊 Размеры (не менять)
- Маркеры: 40px (сервер) → 40px (клиент)
- Миниатюры: 60px (сервер) → 60px (клиент)

---
**Подробности**: см. `SOLUTIONS.md`