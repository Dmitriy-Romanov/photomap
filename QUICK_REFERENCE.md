# PhotoMap Quick Reference

## 🚀 Запуск проекта
```bash
cargo run
# Сервер запустится на http://127.0.0.1:3001 или 3002
```

## 🗂️ Выбор папки (Обзор кнопка)
- **Работает**: ✅ Системный визард открывается
- **Решение**: External helper process `folder_dialog_helper`
- **НЕ ИСПОЛЬЗОВАТЬ**: Прямой rfd в async контексте

## 📱 HEIC Конвертация
- **Маркеры**: `40x40>` - сохраняет пропорции
- **Миниатюры**: `60x60>` - сохраняет пропорции
- **ЗАПРЕЩЕНО**: `40x40^` или `60x60^` (искажает пропорции)

## 📁 Структура проекта
```
photomap/
├── src/
│   ├── main.rs                 # Основная логика
│   ├── server.rs               # HTTP API
│   ├── folder_picker.rs        # Выбор папок (external helper)
│   ├── image_processing.rs     # HEIC обработка (правильные параметры)
│   └── html_template.rs        # Web интерфейс
├── folder_dialog_helper/       # Helper для RFD
├── SOLUTIONS.md                 # Подробная документация
└── photos/                     # Папка с фотографиями
```

## 🔧 Критически важные проверки

### 1. Проверить helper program
```bash
cd folder_dialog_helper
cargo build --release
./target/release/folder_dialog_helper
# Должен открыть системный диалог
```

### 2. Проверить HEIC параметры
В `src/image_processing.rs` убедиться:
```rust
"marker" => vec![..., "-resize", "40x40>", ...]
"thumbnail" => vec![..., "-resize", "60x60>", ...]
```

### 3. Проверить API
```bash
curl http://127.0.0.1:3001/api/select-folder
# Должен вернуть путь выбранной папки
```

## 🚨 Запрещенные подходы
1. ❌ rfd::FileDialog::new().pick_folder() в async
2. ❌ spawn_blocking для rfd на macOS
3. ❌ ImageMagick параметры с `^`
4. ❌ Изменение размеров без синхронизации

## 📊 Размеры (не менять)
- Маркеры: 40px (сервер) → 40px (клиент)
- Миниатюры: 60px (сервер) → 60px (клиент)

---
**Подробности**: см. `SOLUTIONS.md`